// Copy this file to secrets.ts (gitignored) and fill in real values.
// secrets.ts is required for the app to build — see CLAUDE.md.

export const DIRECTIONS_API_KEY = 'YOUR_DIRECTIONS_API_KEY_HERE';
